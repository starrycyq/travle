# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx1024m -Dfile.encoding=UTF-8

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true

# Suppress jlink related warnings
# android.suppressUnsupportedCompileSdk=34

# Disable jlink related warnings
# android.suppressUnsupportedCompileSdk=34
android.experimental.enableNewResourceShrinker=false
# Disable offline repo to avoid jdk image issues
android.enableJetifier=true
# Disable JDK image transformation that causes the jlink error
android.disableResourceValidation=true
android.suppressUnsupportedCompileSdk=34

# Use environment variable for Java home, fallback to system default if not set
# This allows local override while letting CI/CD use default
# org.gradle.java.home=D:\\Program Files\\Microsoft\\jdk-17.0.17.10-hotspot
# Additional settings to enforce JDK usage
# org.gradle.java.home.strict=true

# Additional Gradle JVM args specifically for Android build tools
android.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Disable proxy to avoid network issues
systemProp.http.proxyHost=
systemProp.http.proxyPort=
systemProp.https.proxyHost=
systemProp.https.proxyPort=
systemProp.http.nonProxyHosts=*

# For local development, you can uncomment the next line and adjust the path as needed
# For CI/CD environments, leave it commented to use the default JAVA_HOME
# org.gradle.java.home=/path/to/your/local/jdk